const toolbar = {
    options: ['inline'],
    inline: {
        bold: { className: 'custom-icon' },
        italic: { className: 'custom-icon' },
        options: ['bold', 'italic'],
    }
}

export default toolbar;
